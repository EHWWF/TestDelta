alter table cost_ltc_fct add (study_wbs_id nvarchar2(100));
prompt -- After creating VIEW run for testin: select * from cost_ltc_fct_vw where root_parent_wbs_id is not null;
prompt --
prompt --
prompt --ERROR, FEHLER: it just a note for deployment that manual task must be done, see: 20161215_Manual_ltc_study_wbs_id.TXT
prompt --
prompt --
prompt --